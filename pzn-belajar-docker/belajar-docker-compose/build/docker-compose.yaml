version: "3.8"

services:
    app:
        container_name: app
        build:
            context: "./app"
            dockerfile: Dockerfile
        image: "app-golang:1.0.0"   # Ini adalah image hasil dari build pada proses diatasnya
        environment:
            - "APP_PORT=8080"
        ports:
            - "8080:8080"
